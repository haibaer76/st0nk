<h1>Alle Dokumente</h1>

<table border="0">
  <% @documents.each do |document| %>
    <tr>
      <td><%= link_to document.human_name, "docs/#{document.name}.root" %></td>
    </tr>
  <% end %>
</table>
<%= link_to "Neues Dokument anlegen", {:controller => :documents, :action => :new} %>

