<h1>Documents#index</h1>

<table border="1">
  <thead><tr>
      <th>Name</th>
  </tr></thead>
  <tbody>
    <% @documents.each do |doc| %>
      <tr><td><%= link_to doc.name, "/documents/show/#{doc.id}" %></td></tr>
    <% end %>
  </tbody>
</table>

<%= link_to "Create new", "/documents/new" %>

